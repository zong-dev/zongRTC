import { Create } from '../components/CreateButton';

export const Home = () => {
    return (
        <div className="App flex items-center w-screen h-screen justify-center">
            <Create />
        </div>
    );
}